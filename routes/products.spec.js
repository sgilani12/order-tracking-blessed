let customers = require('./products');
let expect = require('chai').expect;

describe('products', () => {
    it("should pass this test in products", () => {
        //nothing means pass        
    })

})