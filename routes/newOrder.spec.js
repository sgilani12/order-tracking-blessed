let customers = require('./newOrder');
let expect = require('chai').expect;

describe('newOrder', () => {
    it("should pass this test in newOrder", () => {
        //nothing means pass        
    })

})